# 教学课件排版定制规范与跨设备迁移说明书
> **适用项目**：南开大学《Python爬虫开发：从入门到实战》及通用教学课件制作  
> **基础模板**：PPT Master - Google Brand 模板  
> **适用工具**：VSCode + Claude Code 插件 / Antigravity / 其它大模型智能体  

---

## 一、 背景与目的

本项目在生成第 1~6 章课件的过程中，针对课堂投影、讲授节奏以及视觉层次进行了多次精细调校。为了保证换用其他电脑或开启全新会话时，**后续章节能无缝继承这套经过验证的审美风格与版式规范**，特将全部定制参数与迁移方法整理归档于本文档。

---

## 二、 核心排版与审美规范（精确参数表）

在后续生成任何新章节（如第 7 章及之后）或修改历史章节时，必须严格执行以下规则：

### 1. 全局字体与字号阶梯（Typography）

* **强制中文字体**：全局统一使用 **微软雅黑（`Microsoft YaHei`）**，完全替换 Google 模板默认的 Segoe UI。
* **字号锚点（像素 / 磅值对照）**：
  * **正文（body）**：`24px`
  * **主标题（title）**：`32px`（加粗 bold）
  * **副标题（subtitle）**：`32px`（常规/粗体）或 短说明 `24px`
  * **代码文本（code）**：`22px`（等宽/代码块）
  * **标注说明（annotation）**：`18px`
  * **页脚/页码（footnote）**：`16px`
  * **章节大数字（section_number）**：`200px`（加粗 bold）

---

### 2. 正文内容页页眉体系（Content Page Header）

内容页遵循自研的**左上角两行双轨标题体系**（经多次对比，否决了 18/42 与 22/36 方案，最终锁定的 1.26:1 黄金比例）：

* **左上角双行标题**：
  * **第 1 行（章节/小节标签）**：`25.33px`（即 `19pt`）加粗，颜色为 Google 蓝 `#4285F4`，垂直坐标约 `y=58`。
  * **第 2 行（页面主标题）**：`32px`（即 `24pt`）加粗，颜色为深灰黑 `#202124`，垂直坐标约 `y=96`。
  * **页眉整体包围盒（bounds）**：约 `40 28 1120 96`。
* **顶部品牌装饰条**：页面最顶端放置一条 `1280×6` 细条，颜色为 Google 蓝 `#4285F4`（封面/封底使用 14px 四色分段条）。
* **严禁包含谷歌图标（强制红线）**：页面中（包括封面、封底、内容页右上角等所有区域）一律严禁放置任何谷歌品牌图标（如 `google_g_logo.svg`、`google_wordmark.svg`），保持纯净的现代无徽标教学版式。右上角保持留白。
* **底部页脚（Footer）**：
  * 字号 `16px`，颜色 `#9AA0A6`。
  * 左侧对齐显示章节归属（如：`第X章 · 章节名称`）。
  * 右侧对齐显示页码（如：`NN`，属性 `x=1240 text-anchor="end"`）。

---

### 3. 章节过渡页固定版式（Section Page - 继承第 2 章 P05 样式）

所有的章节标题/小节过渡页（Section Opener），一律采用以下版式规范：

* **画布结构**：
  * 纯白底色画布。
  * **左侧浅色区域**：宽 `520px`，高度填满（全高 `720px`）。
  * **最左侧主色装饰条**：宽 `14px`，贯通上下。
  * **主视觉大数字**：在左侧色块内放置 `200px` 主色超大粗体数字（锚点 `x=100 y=380`，bounds 约 `80 180 320 260`）。
  * **标题文案区**：大数字下方放置小节主标题（`32px` 加粗 `#202124`）+ 副标题（`24px` `#5F6368`，字数精简 ≤ 16 字）。
* **右侧任务卡组（Task Cards）**：
  * 右侧区域放置该小节包含的子任务卡片。
  * 卡片规范：白底圆角卡片，尺寸 `600×96`，圆角 `rx=16`。
  * 卡片内容：`40px` 主色图标 + `24px` 加粗任务名（`#202124`）+ `18px` 任务简介说明（`#5F6368`）。
* **右下角总结卡（Footer Note）**：
  * 配备 `36px` 主色灵感灯泡图标（`bulb`）+ `20px` 单行核心提示语（`#202124`，≤ 20 字）。
* **小节四色轮换方案（按节号依次循环）**：
  * **01 蓝**：底块 `#E8F0FE` · 竖条/数字/图标 `#4285F4`
  * **02 绿**：底块 `#E6F4EA` · 竖条/数字/图标 `#34A853`
  * **03 黄**：底块 `#FEF7E0` · 竖条 `#FBBC05` · 数字/图标 `#F9AB00`
  * **04 红**：底块 `#FCE8E6` · 竖条/数字/图标 `#EA4335`

---

### 4. 随堂习题/测验互动动效规范（Quiz & Exercise Interactive Animation）

为了保障教学互动质量与随堂测验的真实练习意义，针对所有习题、测验、客观选择题页面，**强制执行原生动效规范**：

* **严禁提前泄露答案（强制教学红线）**：
  * 进入习题幻灯片时，**绝对禁止在初始未点击状态直接标出正确答案或展示答案解析**；
  * 若一开始就直接高亮答案，学生无需思考即可看到结果，彻底丧失随堂练习与师生互动的意义。
* **初始呈现状态（未点击状态）**：
  * 题目序号徽章、题干文本及所有选项（A、B、C、D）一律以**中性浅灰底色**（`#F1F3F4`）和常规边框（`#DADCE0`）呈现；
  * 正确选项不得有任何颜色高亮或字体加粗区别；
  * 底部的【答案与解析栏】**默认完全隐藏**；
  * 留出充分时间供学生独立审题、思考作答或课堂讨论。
* **分步点击揭晓动效（Step-by-step On-Click Animation）**：
  * 必须使用 PowerPoint 原生淡入动画（`entrance_fade`，时长 `0.25s`）：
    * **第 1 次点击（Click 1 · 揭晓第 1 题）**：第 1 题正确选项平滑淡入覆盖为绿色高亮卡片（浅绿底 `#E6F4EA`、深绿边框 `#34A853`、加粗绿字），同时底部的答案与详细解析条同步伴随淡入（`with-previous`）；
    * **第 2 次点击（Click 2 · 揭晓第 2 题）**：若为多题页面（如双题并列版式），第 2 次点击再揭晓第 2 题的正确选项高亮与答案解析条；
    * **再次点击**：平滑切入下一页。
* **底层实现标准**：
  * 通过标准 OpenXML `<p:timing>` 序列节点，为正确选项高亮层与解析条绑定 `on-click` + `with-previous` 级联时序，原生支持 PowerPoint 与 WPS 放映模式。

---

## 三、 专属品牌解耦架构（彻底避免 Git 冲突）


为了在享受原作者更新的同时互不干扰，本规范已解耦为**独立专属品牌模板**：

* **专属品牌目录**：
  `skills/ppt-master/templates/brands/google-teaching/`
* **模板规格定义**：
  `skills/ppt-master/templates/brands/google-teaching/templates/design_spec.md`
* **品牌注册记录**：已注册至 `skills/ppt-master/templates/brands/brands_index.json`（ID 为 `google-teaching`）。
* **解耦收益**：
  * 原作者自带的 `google` 原版模板已保持 100% 纯净未改动。
  * 原作者的远程 Git 仓库中不存在 `google-teaching` 目录，因此后续执行 `git pull --rebase` 时**永远不会发生合并冲突**！

---

## 四、 换新电脑时的迁移与恢复指南

换到新电脑后，可通过以下方式快速恢复全部配置与记忆：

### 方案 A：整包拷贝 / Git 同步（最简推荐）

1. **直接复制整个项目文件夹**：
   将 `PPT-master` 项目目录直接复制到新电脑的任意磁盘（例如 `D:\code\Python_files\PPT-master\`）。
2. **安装依赖**：
   打开终端进入 `ppt-master` 目录，执行：
   ```bash
   pip install -r requirements.txt
   ```
3. **配置 API Key**：
   确保你的 DeepSeek API Key 已配置。可在环境变量中设置 `DEEPSEEK_API_KEY`，或在 VSCode 插件中填入。
4. **自动生效机制**：
   因为 `CUSTOM_STYLE_SPEC.md`、`google-teaching` 品牌模板和 `AGENTS.md` 规约均已直接保存在代码仓库中，**在新电脑上无论开启哪个新对话，AI 都会自动加载并应用这套审美规则**！

---

### 方案 B：恢复 Claude Code 插件记忆（Memory）

如果你希望在新电脑上完全保留 Claude Code 的本地记忆缓存，只需将原电脑中的记忆文件复制到新电脑：

* **原电脑记忆路径**：
  `C:\Users\<用户名>\.claude\projects\d--code-Python-files-PPT-master-ppt-master\memory\`
* **需复制的文件**：
  * `google-brand-teaching-style.md`（课件版式记忆）
  * `crawler-textbook-source.md`（教材源文件路径配置）
  * `MEMORY.md`（记忆索引）
* **粘贴到新电脑**：
  新电脑上启动一次 Claude Code 后，会在 `C:\Users\<新用户名>\.claude\projects\` 下生成对应的项目目录，将上述文件复制进去即可。

---

### 方案 C：全新环境 / 第三方 AI 一键激活 Prompt（备用）

如果在没有任何历史配置的全新电脑或第三方模型界面中开始生成课件，只需将以下提示词直接发送给 AI 即可立刻生效：

```markdown
请为我制作《Python爬虫开发：从入门到实战》的教学课件。
制作时必须严格执行以下定制版式与审美规范（继承已确认的标准）：
1. 全局字体：一律使用微软雅黑（Microsoft YaHei），禁止使用 Segoe UI。
2. 字号锚点：正文 24px、标题 32px(粗体)、代码 22px、标注 18px、页脚 16px、章节大数字 200px。
3. 内容页页眉：左上角两行体系——第一行章节标签 25.33px(19pt)加粗蓝色(#4285F4)；第二行页面标题 32px(24pt)加粗深色(#202124)；右上角放置 24x24 google_g_logo.svg；顶部为 1280x6 蓝色细条；页脚 16px 灰色显示章节名与页码。
4. 章节过渡页：严格遵循第2章P05版式——白底，左侧 520px 浅色块 + 14px 主色竖条 + 200px 主色大数字 + 32px标题/24px副标题；右侧为白底圆角任务卡(600x96, rx=16)；右下角 bulb 图标+提示语；四小节颜色依序按 蓝(#4285F4)、绿(#34A853)、黄(#FBBC05)、红(#EA4335) 轮换。
```

---

## 五、 历史已完成章节归档索引

新电脑中若需查阅或参考先前生成的源文件与样式样本，可直接查看各工程目录：

| 章节 | 工程目录名 | 说明 |
| :--- | :--- | :--- |
| **第 1 章** | `projects/python项目/crawler-ch1_ppt169_20260802` | 基础环境搭建与 requests 初探 |
| **第 2 章** | `projects/python项目/crawler-ch2_ppt169_20260803` | 数据解析（BeautifulSoup / XPath），**课件审美风格发源版** |
| **第 3 章** | `projects/python项目/crawler-ch3_ppt169_20260804` | 静态网页采集实战（豆瓣 Top250） |
| **第 4 章** | `projects/python项目/crawler-ch4_ppt169_20260806` | 动态网页与逆向分析 |
| **第 5 章** | `projects/python项目/crawler-ch5_ppt169_20260807` | 数据存储与高效处理（42页纯代码与表格） |
| **第 6 章** | `projects/python项目/crawler-ch6_ppt169_20260807` | 验证码识别与高级反爬对抗 |
